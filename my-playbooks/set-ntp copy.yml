---
- hosts: localhost
  name: Set NTP DSC

  tasks:
    - vmware_host_ntp:
        hostname: "vc.virtuallywired.io"
        username: "administrator@vsphere.local"
        password: "VMware1!"
        validate_certs: no
        cluster_name: "vSphere-Cluster"
        ntp_servers:
          - 10.10.10.30